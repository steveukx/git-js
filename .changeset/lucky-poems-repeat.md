---
"simple-git": patch
---

Publish separate type declarations per module format so `import simpleGit from 'simple-git'` is callable under `node16` module resolution, and add types for `simple-git/promise`.

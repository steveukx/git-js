name: CI

on:
   push:
      branches: [main]
   pull_request:
      branches: [main]

permissions:
   contents: read

jobs:
   build-ubuntu:
      runs-on: ubuntu-latest
      strategy:
         matrix:
            node-version: [20, 22, 24, 25, 26]
      steps:
         - uses: actions/checkout@v7
         - uses: pnpm/setup@v2
           with:
              runtime: node@${{ matrix.node-version }}
              cache: true
         - run: pnpm build
         - name: Test
           env:
              GIT_AUTHOR_NAME: 'Simple Git Tests'
              GIT_AUTHOR_EMAIL: 'tests@simple-git.dev'
           run: pnpm test

   build-windows:
      runs-on: windows-latest
      steps:
         - uses: actions/checkout@v7
         - uses: pnpm/setup@v2
           with:
              runtime: node@24
              cache: true
         - run: pnpm build
         - name: Test
           env:
              GIT_AUTHOR_NAME: 'Simple Git Tests'
              GIT_AUTHOR_EMAIL: 'tests@simple-git.dev'
           run: pnpm test:win

   consumer:
      runs-on: ubuntu-latest
      strategy:
         matrix:
            node-version: [ 22, 24, 26 ]
      steps:
         - uses: actions/checkout@v7
         - uses: pnpm/setup@v2
           with:
              runtime: node@${{ matrix.node-version }}
              cache: true
         - run: pnpm build && pnpm build:pkg
         #      Intentionally allowing the lockfile to change after the
         #      build:pkg step has rewritten package.json files to point
         #      to built content.
         - run: pnpm i --no-frozen-lockfile
         - run: pnpm test:consumer
